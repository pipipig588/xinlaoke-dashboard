# 新老客 Dashboard — 位置速查（换对话窗口先看这个）

> 直播间销售 / 新老客分析 Streamlit 仪表盘。换新对话找不到东西时，对照本表即可。

## 📍 核心位置

| 用途 | 路径 / 地址 |
|---|---|
| **本地主目录（改代码、跑程序都用这个）** | `/Users/anirv/Downloads/xinlaoke/` |
| GitHub 备份（main 分支，已同步） | https://github.com/pipipig588/xinlaoke-dashboard |
| 部署压缩包（给公司 Windows 电脑） | `/Users/anirv/Documents/报表订单/xinlaoke-dashboard.zip` |
| 旧版去重逻辑备份 | `/Users/anirv/Downloads/xinlaoke/preprocess_旧版去重逻辑_备份_20260615.py` |

> ⚠️ 主目录下的 `.claude/worktrees/...` 是 git 临时工作副本，**不是最新代码，别去那里改**。认准 `/Users/anirv/Downloads/xinlaoke/`。

## 📂 数据目录（gitignore，不进 GitHub / 压缩包）

| 源表（放 `data/raw/`） | 列 | 对应 Tab | 必需? |
|---|---|---|---|
| `报表订单.xlsx` | 3 sheet：23-24/25/26 | 全部订单类 Tab | ✅ 必需 |
| `派样.xlsx` | 同报表订单结构 | 🧪 Sample回购 | 可选 |
| `会员.xlsx` | 淘宝ID、入会时间 | 🪪 会员 | 可选 |
| `人群.xlsx` | 人群名称、淘宝ID | 👥 人群 | 可选 |

- 预处理产物在 `data/processed/`：`orders.parquet` / `purchase_pairs.parquet` / `samples.parquet` / `members.parquet` / `crowds.parquet` / `meta.json`
- **换了源表必须重跑**：`cd /Users/anirv/Downloads/xinlaoke && python3 preprocess.py`
- 别删 `data/raw/` 和 `data/processed/`（删了 git 救不回来）

## 🗂 关键代码文件（主目录下）

| 文件 | 作用 |
|---|---|
| `app.py` | Streamlit 主程序，11 个 Tab |
| `config.py` | 字段映射 COLUMN_MAP + 多表关键词 |
| `preprocess.py` | Excel → Parquet 预处理（8 步，多表） |
| `README.md` / `PROJECT_STATUS.md` | 部署说明 / 完整项目文档 |
| `启动仪表盘.command` / `.bat` | Mac / Windows 一键启动 |

## ▶️ 启动 & 访问

- Mac 启动：双击 `启动仪表盘.command`（或 `cd` 进主目录跑 `streamlit run app.py`）
- 本机访问：http://localhost:8501
- Tailscale 远程：http://100.106.228.108:8501

## 11 个 Tab（顺序）

渠道汇总 · 新老客趋势 · 货品分析 · RFM · 渠道流转 · 复购率 · 复购周期 · **Sample回购** · **会员** · **人群** · 平台优惠

---
_最后更新：2026-06-18（main commit a6274aa 时）。改了代码记得同步更新本表。_
